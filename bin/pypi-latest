#!/bin/sh
#
#   Get the latest version of a package from PyPI.
#
#   Usage: pypi-latest <package>
#
#   Requires: curl, jq
#
#   Example: pypi-latest django
#
curl -s https://pypi.org/pypi/$1/json | echo "$1 >=$(jq -r .info.version)"
