#!/bin/sh
#
# Nukes a branch locally and on the origin remote. Also prunes the origin.
#
# $1 - Branch name.
#
# Examples
#
#   git nuke add-git-nuke

git branch -D $1
git push origin :$1
git remote prune origin
